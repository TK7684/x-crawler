# Pending Items — 2026-04-13

## From: x-crawler /forward (unified-crawler-db-wsl-ec2-rollout session)

### Filed as GitHub issue
- [ ] **Rewrite `resolve_names.py`** → fb-crawler#1 (https://github.com/TK7684/fb-crawler/issues/1)
  - Use `og:title` first, fall back to `/groups/{id}/about`, fail loud on miss
  - Add content sanity check: fail batch if >80% of values are identical

### Not filed (user declined)
- [ ] Delete legacy `.bak` files on both hosts — deferred until ~1 week confidence period elapses
- [ ] Decide systemd service fate on EC2 — delete `/etc/systemd/system/{fb-crawler,x-scraper}.service` or keep as disabled manual fallback
- [ ] Rename EC2 project dirs `x-scraper` → `x-crawler`, `fb-group-scraper` → `fb-crawler` — blocks `git pull`-based deploys
- [ ] Delete zero-byte shell DB files (`scraper.db`, `x_profiles.db`) in both project dirs

### Monitor-type items (skipped by rrr/forward rules)
- [ ] Inspect first EC2 cron tick post-deploy — verify Discord notifications render correctly
- [ ] Consider adding `combined.db` that ATTACHes both WSL and EC2 unified DBs for cross-host analytics
